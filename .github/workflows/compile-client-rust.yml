name: Compile Client Rust

on:
  push:
    branches: [ main ]
    paths:
      - 'legacy/client-rust/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'legacy/client-rust/**'

jobs:
  compile:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        override: true
        components: rustfmt, clippy
        target: armv7-unknown-linux-gnueabihf
    
    - name: Install cross
      run: cargo install cross
      
    - name: Compile client-rust
      working-directory: legacy/client-rust
      run: cross build --release --target armv7-unknown-linux-gnueabihf
    
    - name: Upload compiled binary
      uses: actions/upload-artifact@v4
      with:
        name: client-rust-binary
        path: legacy/client-rust/target/armv7-unknown-linux-gnueabihf/release/client
        retention-days: 7
